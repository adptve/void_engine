# void_engine examples
#
# Note: model-viewer is a data-driven example (TOML + assets only)
# It is run using: void_runtime examples/model-viewer

# Find OpenGL for any future examples
find_package(OpenGL REQUIRED)
