[package]
name = "void_math"
version.workspace = true
edition.workspace = true
description = "SIMD-optimized math library for the Metaverse Engine"

[features]
default = ["std"]
std = []
simd = []  # Enable explicit SIMD intrinsics
serde = ["dep:serde"]

[dependencies]
void_core = { workspace = true }
serde = { version = "1.0", default-features = false, features = ["derive"], optional = true }

[dev-dependencies]
bincode = { workspace = true }
