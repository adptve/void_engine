[package]
name = "void_kernel"
version.workspace = true
edition.workspace = true
description = "Core kernel for Void Engine - orchestrates frame assembly and layer composition"

[dependencies]
# Internal crates
void_core = { workspace = true }
void_ecs = { workspace = true }
void_ir = { workspace = true }
void_render = { workspace = true }
void_asset = { workspace = true }

# Concurrency
parking_lot = { workspace = true }
crossbeam-channel = { workspace = true }

# Logging
log = { workspace = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }
bincode = { workspace = true }
toml = "0.8"

# Error handling
thiserror = { workspace = true }

# Signal handling (Unix only)
[target.'cfg(unix)'.dependencies]
ctrlc = "3.4"
